# Release notes

## 0.4.1

Fixes:

* Uses `flywheel/python-gdcm:sse` as base Docker image for more robust source DICOM
  handling

Maintenance:

* Updates the `.gitlab-ci.yml` and `pre-commit-config.yaml` to current versions.
* Updates `lxml` to `^5.3`
* Adds `pylibjpeg` (`^2.0`)
* Fixes a couple of broken links
* Adds these Release notes
